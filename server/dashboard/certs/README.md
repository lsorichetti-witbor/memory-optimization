# Local TLS-interception root certs (corporate proxy / antivirus).
# Empty by default. Drop a single-cert .crt file here when your network
# re-signs HTTPS; server/dev.Dockerfile installs whatever it finds.
